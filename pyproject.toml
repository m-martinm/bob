[project]
name = "bob"
version = "0.1.0"
description = "A python build system."
readme = "README.md"
requires-python = ">=3.8"
license = "MIT"

[build-system]
requires = ["setuptools"]
build-backend = "setuptools.build_meta"
